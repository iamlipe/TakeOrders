import React from 'react';
import styled from 'styled-components/native';

export const StockHome = () => {
  return (
    <StyledContainer>
      <StyledText>StockHome</StyledText>
    </StyledContainer>
  );
};

const StyledContainer = styled.View`
  min-height: 100%;
`;

const StyledText = styled.Text``;
