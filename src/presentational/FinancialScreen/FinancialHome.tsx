import React from 'react';
import styled from 'styled-components/native';

export const FinancialHome = () => {
  return (
    <StyledContainer>
      <StyledText>FinancialHome</StyledText>
    </StyledContainer>
  );
};

const StyledContainer = styled.View`
  min-height: 100%;
`;

const StyledText = styled.Text``;
